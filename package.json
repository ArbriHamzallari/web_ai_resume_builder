{
  "name": "web-ai-resume-builder",
  "version": "1.0.0",
  "description": "AI Resume Builder - Full Stack Application",
  "scripts": {
    "build": "cd client && npm install && npm run build"
  },
  "engines": {
    "node": ">=18.x"
  }
}

